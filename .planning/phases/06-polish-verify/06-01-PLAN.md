---
phase: 06-polish-verify
plan: 01
type: execute
---

<objective>
Final consistency checks and browser verification.

Purpose: Ensure design system applied consistently, no regressions
Output: Verified production-ready app
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-phase.md
@./.claude/get-shit-done/templates/summary.md
@./.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-wizard-redesign/05-03-SUMMARY.md

**From Phase 5:**
- Typography: Oswald headings, Poppins body
- All 15 wizard components redesigned
- Browser warning added for restore

**Design system:**
- font-display: Oswald for h1-h6
- font-body: Poppins for UI text
- Tailwind default spacing
</context>

<tasks>

<task type="auto">
  <name>Task 1: Typography consistency audit</name>
  <files>src/components/**/*.jsx, src/pages/**/*.jsx</files>
  <action>Grep for font usage across all components. Check:
  - All headings use font-display
  - All body text uses font-body
  - No hardcoded font-family values
  - No font-bold without font-display/font-body

Flag any inconsistencies found.</action>
  <verify>Grep results show consistent typography usage</verify>
  <done>All text elements use design system font classes</done>
</task>

<task type="auto">
  <name>Task 2: Build and bundle check</name>
  <files>N/A</files>
  <action>Run production build. Check bundle sizes reasonable (<200KB main bundle), no console warnings. Verify all components compile without errors.</action>
  <verify>npm run build succeeds, dist/ contains optimized assets</verify>
  <done>Build passes, bundle sizes acceptable, no warnings</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete redesigned app with design system</what-built>
  <how-to-verify>
1. Run: npm run dev
2. Test backup flow:
   - Home → Create Backup → Device selector
   - Scan device → Library overview → Config → Progress → Success
   - Verify typography: headings use Oswald, body uses Poppins
   - Check spacing: consistent padding/margins
3. Test restore flow:
   - Home → Restore → File upload → Preview → Options → (don't actually restore)
   - Verify browser warning displays
   - Check all text readable, buttons clickable
4. Cross-browser spot check (if available):
   - Chrome: Primary test
   - Firefox: Check if UI renders correctly
   - Edge: Verify compatibility warning works
5. Responsive check:
   - Desktop (1920px): Full layout
   - Tablet (768px): Should adapt reasonably
   - Mobile (375px): Should be usable
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- [ ] npm run build succeeds
- [ ] Typography consistent (grep results clean)
- [ ] All flows tested manually
- [ ] No console errors
</verification>

<success_criteria>
- Typography audit complete
- Build passes without warnings
- All flows verified working
- Phase 6 complete, project ready for deployment
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-verify/06-01-SUMMARY.md`:

# Phase 6 Plan 1: Polish & Verify Summary

**Final verification complete - production ready**

## Accomplishments
- Typography audit
- Build verification
- Manual testing

## Files Created/Modified
[List any fixes made]

## Decisions Made
None

## Issues Encountered
[Any issues found and fixed, or "None"]

## Next Phase Readiness
Project complete. Ready for deployment.
</output>
