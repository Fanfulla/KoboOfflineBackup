---
phase: 04-core-components
plan: 01
type: execute
---

<objective>
Redesign Button and Card components with new design system - professional typography, standard spacing, clean aesthetics.

Purpose: Apply Phase 3 design foundation to the most fundamental UI components, establishing patterns for remaining components.
Output: Redesigned Button and Card with simplified styles, proper spacing, readable text sizes.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-design-system/03-01-SUMMARY.md
@src/components/common/Button.jsx
@src/components/common/Card.jsx
@tailwind.config.js

**Design system changes (Phase 3):**
- Typography: text-sm now 14px (was 10px), text-base now 16px (was 13px), text-lg now 18px (was 16px)
- Spacing: p-3 now 12px (was 24px), p-4 now 16px (was 32px), p-6 now 24px (was 48px)
- Font: Oswald active for all text

**Current component issues:**
- Button: padding too large (p-6 = 48px before, now 24px but still needs review), hover effects heavy
- Card: padding p-6 needs review, elevation/glass variants working but can be simplified
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign Button component with professional spacing and simplified styles</name>
  <files>src/components/common/Button.jsx</files>
  <action>
Update Button component to use new design system:

**Sizing adjustments (spacing scale changed):**
- sm: 'px-3 py-1.5 text-sm' (12px/6px padding, 14px text)
- md: 'px-4 py-2 text-base' (16px/8px padding, 16px text - DEFAULT)
- lg: 'px-6 py-3 text-lg' (24px/12px padding, 18px text)

**Simplify visual styles:**
- Remove heavy hover effects: no scale-105, no shadow-xl growth
- Keep: rounded-lg (not xl - less extreme), subtle transitions
- Primary: bg-kobo-accent hover:bg-kobo-accent-dark (simple color shift)
- Secondary: border-kobo-gray-light hover:bg-kobo-gray-light/10 (subtle highlight)
- Ghost: text-kobo-accent hover:bg-kobo-accent/10 (minimal)
- Danger: bg-kobo-error hover:bg-kobo-error/90 (simple)

**Why these changes:**
- Spacing scale changed: old padding values too large with new scale
- Professional feel: subtle interactions, not flashy animations
- Oswald font already applied via Tailwind config

**DO NOT:**
- Add new variants or features - focus on applying design system
- Use old spacing values (p-6 was 48px, now 24px - too much)
- Keep heavy hover effects (scale, excessive shadows)
  </action>
  <verify>
1. Check Button.jsx uses new sizing (px-3/4/6, py-1.5/2/3)
2. Check rounded-lg (not rounded-xl)
3. Check no scale- or shadow-xl in hover states
4. Run: npm run build -- succeeds without errors
  </verify>
  <done>Button padding appropriate for new spacing scale, hover effects subtle and professional, no build errors</done>
</task>

<task type="auto">
  <name>Task 2: Redesign Card component with cleaner spacing and simplified variants</name>
  <files>src/components/common/Card.jsx</files>
  <action>
Update Card component to use new design system:

**Spacing adjustments:**
- Default padding: p-6 (24px with new scale - appropriate for cards)
- Keep rounded-2xl (large cards benefit from generous rounding)

**Simplify variants:**
- elevated: 'bg-white shadow-elevated' (use Tailwind config shadow)
- glass: 'bg-kobo-glass backdrop-blur-sm' (use config color)
- flat: 'bg-kobo-cream-dark' (simple, no shadow)

**Remove complexity:**
- Keep gradient prop working (subtle accent, user may use it)
- Simplify baseStyles: just 'rounded-2xl p-6 relative'
- Remove overflow-hidden if not needed (check if gradient uses it - if yes, keep)

**Why these changes:**
- p-6 with new scale = 24px (good card padding)
- Leverage Tailwind config values (shadow-elevated, kobo-glass)
- Professional: clean, not overdone

**DO NOT:**
- Change card structure (relative/z-10 needed for gradient overlay)
- Add new variants
- Remove gradient feature (may be used in UI)
  </action>
  <verify>
1. Check Card.jsx uses p-6, rounded-2xl
2. Check variants use Tailwind config values (shadow-elevated, bg-kobo-glass)
3. Check gradient overlay still works if gradient prop true
4. Run: npm run build -- succeeds without errors
  </verify>
  <done>Card padding appropriate, variants clean and using design system tokens, gradient feature intact, no build errors</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds
- [ ] Button sizing looks reasonable (not too cramped, not too large)
- [ ] Card padding looks appropriate
- [ ] No TypeScript/ESLint errors introduced
</verification>

<success_criteria>
- Button and Card redesigned with new spacing scale
- Hover effects simplified (professional, not flashy)
- Components use design system tokens from Tailwind config
- No build errors
- Code follows existing conventions (PropTypes, exports)
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-components/04-01-SUMMARY.md`:

# Phase 4 Plan 1: Foundation Components Summary

**Button and Card redesigned - professional spacing, clean interactions.**

## Accomplishments

- Redesigned Button with new spacing scale and subtle hover effects
- Redesigned Card with clean variants using design system tokens
- Components now properly sized with Tailwind default spacing

## Files Created/Modified

- `src/components/common/Button.jsx` - New sizing, simplified styles
- `src/components/common/Card.jsx` - Clean variants, proper spacing

## Decisions Made

- Button: Subtle interactions (no scale effects)
- Card: p-6 appropriate for cards with new spacing scale
- Leverage Tailwind config tokens (shadow-elevated, kobo-glass)

## Issues Encountered

[Any issues, or "None"]

## Next Step

Ready for 04-02-PLAN.md (ProgressBar, CircularProgress, StatusBadge)
</output>
