---
phase: 04-core-components
plan: 03
type: execute
---

<objective>
Redesign Modal, Checkbox, and Icon components to complete core component library with design system.

Purpose: Finalize core component redesign with interactive components using professional spacing and clean styling.
Output: Modal with proper overlay, Checkbox with clean states, Icon wrapper component consistent with design system.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-design-system/03-01-SUMMARY.md
@.planning/phases/04-core-components/04-01-SUMMARY.md
@.planning/phases/04-core-components/04-02-SUMMARY.md
@src/components/common/Modal.jsx
@src/components/common/Checkbox.jsx
@src/components/common/Icon.jsx
@tailwind.config.js

**Design system:**
- Typography: readable sizes (text-sm 14px, text-base 16px, text-lg 18px)
- Spacing: Tailwind defaults
- Colors: kobo-overlay (rgba(44, 36, 22, 0.4)), kobo-glass, kobo-accent
- Shadows: shadow-elevated, shadow-elevated-hover

**Patterns established:**
- Subtle interactions (no heavy animations)
- Design system tokens
- Clean, professional aesthetics
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign Modal with professional overlay and spacing</name>
  <files>src/components/common/Modal.jsx</files>
  <action>
Update Modal component with design system:

**Overlay:**
- Background: bg-kobo-overlay backdrop-blur-sm (use config color + blur)
- Full screen: fixed inset-0
- Click to close on overlay (if not already implemented, keep existing behavior)

**Modal content:**
- Background: bg-white (clean, not glass - easier to read)
- Padding: p-6 or p-8 (24px/32px - generous for modal content)
- Rounded: rounded-2xl (consistent with Card)
- Shadow: shadow-elevated-hover (prominent elevation)
- Max width: max-w-md or max-w-lg (appropriate modal sizes)
- Center: flexbox centering (justify-center items-center on overlay)

**Spacing inside:**
- Title/header: mb-4 (16px gap below title)
- Content section: proper spacing between elements
- Footer/actions: mt-6 (24px gap above actions)

**Animation:**
- Subtle fade-in (animate-fade-in if exists, or simple opacity transition)
- No heavy slide/scale effects

**Why:**
- kobo-overlay from design system
- Modal padding appropriate with new spacing scale
- Professional: clean white content, subtle backdrop

**DO NOT:**
- Change modal behavior (open/close logic)
- Add complex animations
- Use glass effect for modal content (harder to read)
  </action>
  <verify>
1. Check Modal uses bg-kobo-overlay with backdrop-blur-sm
2. Check content has bg-white, p-6/p-8, rounded-2xl, shadow-elevated-hover
3. Check spacing inside modal (mb-4, mt-6 for sections)
4. Run: npm run build -- succeeds
  </verify>
  <done>Modal has professional overlay, clean content styling, proper spacing with new scale, no build errors</done>
</task>

<task type="auto">
  <name>Task 2: Redesign Checkbox and Icon components</name>
  <files>src/components/common/Checkbox.jsx, src/components/common/Icon.jsx</files>
  <action>
**Checkbox updates:**
- Size: w-5 h-5 (20px - readable checkbox)
- Border: border-2 border-kobo-gray-light
- Checked state: bg-kobo-accent border-kobo-accent (accent color when checked)
- Rounded: rounded (subtle corner radius)
- Focus ring: focus-visible:ring-2 focus-visible:ring-kobo-accent
- Label spacing: gap-2 (8px between checkbox and label) if label exists
- Label text: text-base (16px, readable)

**Icon updates:**
- Icon.jsx is likely a simple wrapper for SVG icons or icon library
- Size props: w-4 h-4 (sm), w-5 h-5 (md), w-6 h-6 (lg) - 16px/20px/24px
- Color: currentColor (inherits text color from parent)
- Keep simple: just size and className passthrough

**Why:**
- Checkbox: design system colors, proper sizing
- Icon: simple size utilities, inherits color (flexible)
- Professional: subtle, clean form controls

**DO NOT:**
- Add complex icon library integration (keep simple wrapper)
- Change checkbox behavior (only styling)
- Add features beyond design system application
  </action>
  <verify>
1. Check Checkbox uses w-5 h-5, kobo-accent when checked
2. Check Icon has size variants (w-4/5/6)
3. Check both follow design system patterns
4. Run: npm run build -- succeeds
  </verify>
  <done>Checkbox clean with design system colors and proper sizing, Icon simple and flexible, no build errors</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds
- [ ] Modal overlay uses kobo-overlay color
- [ ] Checkbox sizing appropriate (20px visible and clickable)
- [ ] No errors or warnings
- [ ] All 8 core components redesigned (Button, Card, ProgressBar, CircularProgress, StatusBadge, Modal, Checkbox, Icon)
</verification>

<success_criteria>
- Modal redesigned with professional overlay and clean content
- Checkbox uses design system colors and readable sizing
- Icon component simple and flexible
- All core components now follow design system
- Phase 4 complete - core component library redesigned
- No build errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-components/04-03-SUMMARY.md`:

# Phase 4 Plan 3: Interactive Components Summary

**Modal, Checkbox, Icon redesigned - Phase 4 complete, all core components now follow design system.**

## Accomplishments

- Redesigned Modal with kobo-overlay and clean content styling
- Redesigned Checkbox with design system colors and proper sizing
- Redesigned Icon as simple, flexible wrapper
- Phase 4 complete: All 8 core components redesigned

## Files Created/Modified

- `src/components/common/Modal.jsx` - Professional overlay, clean content
- `src/components/common/Checkbox.jsx` - Design system colors, readable sizing
- `src/components/common/Icon.jsx` - Simple size utilities

## Decisions Made

- Modal: White content on kobo-overlay backdrop (readable, professional)
- Checkbox: 20px size (visible and clickable)
- Icon: Inherits color from parent (flexible, simple)

## Issues Encountered

[Any issues, or "None"]

## Next Phase Readiness

Phase 4 complete. All core components (Button, Card, ProgressBar, CircularProgress, StatusBadge, Modal, Checkbox, Icon) now use the design system from Phase 3.

Ready for Phase 5 (Wizard Redesign) to apply these redesigned components to the backup/restore flows.

**Note for Phase 5:** With new component sizing, wizard layouts may need spacing adjustments. All text is now more readable, buttons appropriately sized.
</output>
