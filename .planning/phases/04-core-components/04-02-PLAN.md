---
phase: 04-core-components
plan: 02
type: execute
---

<objective>
Redesign progress indicators and status components with consistent styling and proper spacing.

Purpose: Apply design system to feedback components (progress bars, status badges) for visual consistency.
Output: Clean, professional progress indicators and status badges using design system tokens.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-design-system/03-01-SUMMARY.md
@.planning/phases/04-core-components/04-01-SUMMARY.md
@src/components/common/ProgressBar.jsx
@src/components/common/CircularProgress.jsx
@src/components/common/StatusBadge.jsx
@tailwind.config.js

**Design system:**
- Typography: text-xs (12px), text-sm (14px), text-base (16px)
- Spacing: Tailwind defaults (gap-2 = 8px, p-2 = 8px, etc.)
- Colors: kobo-success, kobo-error, kobo-warning, kobo-info, kobo-accent

**Pattern established (Plan 04-01):**
- Use Tailwind config tokens
- Subtle, professional styling
- Standard spacing scale
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign ProgressBar and CircularProgress with consistent styling</name>
  <files>src/components/common/ProgressBar.jsx, src/components/common/CircularProgress.jsx</files>
  <action>
**ProgressBar updates:**
- Height: h-2 or h-3 (8px/12px - clean progress bar height)
- Background: bg-kobo-gray-light/30 (subtle track)
- Fill: bg-kobo-accent (primary color) with smooth transition
- Keep rounded-full for track and fill
- Optional label: text-sm (14px, readable)
- Remove any heavy shadows or animations (professional, minimal)

**CircularProgress updates:**
- Size props: sm/md/lg mapping to actual pixel sizes (w-8/w-12/w-16 or similar)
- Stroke color: stroke-kobo-accent (use design system color)
- Track color: stroke-kobo-gray-light/30 (consistent with ProgressBar)
- Smooth spin animation (existing animate-spin is fine if used)
- No excessive stroke-width (2-3px appropriate)

**Why:**
- Consistent colors across both progress components
- Professional: subtle, not distracting
- Design system colors (kobo-accent, kobo-gray-light)

**DO NOT:**
- Add features beyond styling cleanup
- Use colors outside design system
- Make progress animations heavy or distracting
  </action>
  <verify>
1. Check ProgressBar uses h-2 or h-3, bg-kobo-accent for fill
2. Check CircularProgress uses stroke-kobo-accent
3. Check both use kobo-gray-light for track/background
4. Run: npm run build -- succeeds
  </verify>
  <done>Progress components styled consistently, using design system colors, clean and minimal, no build errors</done>
</task>

<task type="auto">
  <name>Task 2: Redesign StatusBadge with clean status indicators</name>
  <files>src/components/common/StatusBadge.jsx</files>
  <action>
Update StatusBadge to use design system colors and proper spacing:

**Status variants:**
- success: bg-kobo-success/10 text-kobo-success (light bg, colored text)
- error: bg-kobo-error/10 text-kobo-error
- warning: bg-kobo-warning/10 text-kobo-warning
- info: bg-kobo-info/10 text-kobo-info
- default: bg-kobo-gray-light/20 text-kobo-gray

**Sizing:**
- Padding: px-2.5 py-1 (10px/4px - compact badge)
- Text: text-xs font-medium (12px, Oswald medium weight)
- Rounded: rounded-full or rounded-md (badges often use full)

**Keep simple:**
- No icons unless already present and functional
- No shadows (badges are minimal)
- Inline-flex for proper text alignment

**Why:**
- Design system colors for all statuses
- Light backgrounds with colored text (professional badge pattern)
- Compact sizing appropriate for status indicators

**DO NOT:**
- Add icon support if not already present
- Use colors outside design system (kobo-success, kobo-error, etc.)
- Make badges too large (they're inline indicators)
  </action>
  <verify>
1. Check StatusBadge uses kobo-success/error/warning/info colors
2. Check light backgrounds (color/10 opacity pattern)
3. Check text-xs, compact padding (px-2.5 py-1)
4. Run: npm run build -- succeeds
  </verify>
  <done>StatusBadge clean and minimal, uses design system status colors, appropriate sizing, no build errors</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build succeeds
- [ ] Progress components visually consistent
- [ ] StatusBadge uses design system status colors
- [ ] No errors or warnings
</verification>

<success_criteria>
- ProgressBar and CircularProgress styled consistently
- StatusBadge uses design system status colors (kobo-success, etc.)
- All components clean and professional
- No build errors
- Code follows existing conventions
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-components/04-02-SUMMARY.md`:

# Phase 4 Plan 2: Indicators & Status Summary

**Progress and status components redesigned - consistent colors, clean styling.**

## Accomplishments

- Redesigned ProgressBar and CircularProgress with matching styles
- Redesigned StatusBadge with design system status colors
- All feedback components now visually consistent

## Files Created/Modified

- `src/components/common/ProgressBar.jsx` - Consistent styling
- `src/components/common/CircularProgress.jsx` - Matching progress indicator
- `src/components/common/StatusBadge.jsx` - Design system status colors

## Decisions Made

- Progress components share color palette (kobo-accent, kobo-gray-light)
- StatusBadge uses light backgrounds with colored text (professional pattern)
- Compact sizing for status indicators

## Issues Encountered

[Any issues, or "None"]

## Next Step

Ready for 04-03-PLAN.md (Modal, Checkbox, Icon)
</output>
