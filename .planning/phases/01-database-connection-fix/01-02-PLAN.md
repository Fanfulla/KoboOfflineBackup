---
phase: 01-database-connection-fix
plan: 02
type: execute
---

<objective>
Fix the database connection issue based on root cause identified in DISCOVERY.md, ensuring users can successfully scan their Kobo device and access KoboReader.sqlite.

Purpose: Implement the specific fix for File System Access API path navigation, verify it works with real Kobo devices, and clean up diagnostic code.
Output: Working database connection with no "file not found" errors, passing manual verification.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
./.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-database-connection-fix/DISCOVERY.md
@.planning/phases/01-database-connection-fix/01-01-SUMMARY.md
@src/utils/fileSystem.js
@src/hooks/useKoboDevice.js
@src/utils/errors.js

**From Discovery Phase:**
- Root cause: [Will be populated from DISCOVERY.md during execution]
- Recommended fix: [Will be populated from DISCOVERY.md during execution]

**Constraints:**
- Must maintain backward compatibility with existing code using getFileByPath
- Must handle hidden directories (`.kobo`) correctly
- Must work cross-platform (Windows, macOS, Linux)
- Cannot break existing error handling patterns
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement fix from DISCOVERY.md</name>
  <files>src/utils/fileSystem.js, src/hooks/useKoboDevice.js</files>
  <action>Apply the specific fix recommended in DISCOVERY.md. This may involve: (1) Fixing getFileByPath to handle hidden directories correctly, OR (2) Changing how useKoboDevice calls getFileByPath, OR (3) Adding fallback logic to try alternate paths/approaches, OR (4) Other fix based on discovery findings. Follow the exact recommendations from DISCOVERY.md. Maintain existing error handling - still throw FileSystemError with FS_NOT_FOUND if file genuinely doesn't exist after all attempts. Keep error messages user-friendly.</action>
  <verify>npm run dev succeeds with no TypeScript/ESLint errors</verify>
  <done>Fix implemented following DISCOVERY.md recommendations, code builds successfully</done>
</task>

<task type="auto">
  <name>Task 2: Remove diagnostic logging</name>
  <files>src/hooks/useKoboDevice.js, src/utils/fileSystemDebug.js</files>
  <action>Remove temporary diagnostic code added in 01-01 plan: (1) Remove import of fileSystemDebug in useKoboDevice.js, (2) Remove all logDirectoryContents, findKoboDatabase, and diagnostic console.log calls from scanDevice function, (3) Delete src/utils/fileSystemDebug.js entirely. Restore useKoboDevice.js to clean production state - only the fix should remain, no debug code.</action>
  <verify>grep -r "fileSystemDebug" src/ returns no results, grep -r "\[DEBUG\]" src/ returns no results</verify>
  <done>All diagnostic code removed, production-ready code only</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Fixed database connection for Kobo device scanning</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Visit: http://localhost:5173
    3. Navigate to Backup page
    4. Click "Connect Kobo Device"
    5. Select your Kobo device directory (the root of the mounted Kobo drive)
    6. Verify:
       - No "file not found" error appears
       - "Reading database..." progress stage shows
       - "Analyzing books..." stage follows
       - Device scan completes successfully
       - Books list appears (if device has sideloaded books)
    7. Check browser console - should be clean, no errors
    8. Try disconnecting and reconnecting to test reliability

    **Success criteria:**
    - Database connection succeeds without errors
    - Books/annotations extracted from KoboReader.sqlite
    - UI flows to next wizard step naturally
    - Console is clean (no unhandled errors)

    **If you don't have a Kobo device:** Test with mock directory structure created earlier.
  </how-to-verify>
  <resume-signal>Type "approved" if scan works, or describe any remaining issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Fix implemented based on DISCOVERY.md
- [ ] All diagnostic code removed
- [ ] npm run dev succeeds
- [ ] Manual testing shows successful database connection
- [ ] No console errors during device scan
</verification>

<success_criteria>
- Database connection fix applied and working
- Users can successfully scan Kobo devices
- KoboReader.sqlite found and parsed
- No "file not found" errors
- Diagnostic code fully removed
- Manual verification passed
</success_criteria>

<output>
After completion, create `.planning/phases/01-database-connection-fix/01-02-SUMMARY.md`:

# Phase 1 Plan 2: Database Connection Fix Implementation Summary

**KoboReader.sqlite connection now works - users can successfully scan Kobo devices.**

## Accomplishments

- Implemented fix: [specific change made]
- Tested with real Kobo device
- Database scan completes successfully
- Books and annotations extracted
- Removed all diagnostic code

## Files Created/Modified

- `src/utils/fileSystem.js` - Fixed getFileByPath [or describe other change]
- `src/hooks/useKoboDevice.js` - Cleaned up diagnostic code
- `src/utils/fileSystemDebug.js` - Deleted (temporary diagnostic utility)

## Decisions Made

[Any architectural decisions or approach choices during fix implementation]

## Issues Encountered

[Any unexpected complications during fix or testing]

## Next Phase Readiness

**Phase 1 complete.** Database connection is working. Ready for Phase 2 (Test Coverage).

**Note for Phase 2:** Should add regression test for the fix to prevent this bug from reoccurring. Test should mock File System Access API and verify getFileByPath handles [specific case from fix].
</output>
